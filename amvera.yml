name: magic-house
runtime: static
entrypoint: build/index.html

build:
  - npm install
  - npm run build

artifacts:
  - build/

environment:
  NODE_ENV: production
  PUBLIC_URL: /

resources:
  memory: 512
  cpu: 0.5

domains:
  - magic-house.amvera.io
